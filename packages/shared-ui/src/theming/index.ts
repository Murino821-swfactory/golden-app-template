/**
 * `@tokenwise/shared-ui/theming` — the palettes and the font table, with no React. Node
 * scripts (the template's schema emitter, tests) import this entry, not the root one.
 */
export * from "./color-schemes";
export * from "./font-families";
